import React from 'react';
import ReactDOM from 'react-dom/client';
import UitspraakApp from './UitspraakApp';
import './style.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UitspraakApp />
  </React.StrictMode>
);
